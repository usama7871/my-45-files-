// Declare a constant variable named `quote` of type `string` and initialize it with the value "A person who never made a mistake never tried anything new.".
const quote: string = "A person who never made a mistake never tried anything new.";

// Declare a constant variable named `author` of type `string` and initialize it with the value "Albert Einstein".
const author: string = "Albert Einstein";

// Use string interpolation to create a message that combines the `quote` and `author` variables. Print the message to the console.
console.log(`${author} once said, "${quote}"`);
