// Declare a constant array named `array` of type `number` and initialize it with the values 1, 2, and 3.
const array: number[] = [1, 2, 3];

// Access the element at index 3 of the array and print it to the console.
console.log(array[3]); // This intentionally causes an array index error
