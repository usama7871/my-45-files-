// Declare a variable named `name` of type `string` and initialize it with the value "Eric".
const name: string = "Eric";

// Use string interpolation to create a greeting message using the value of the `name` variable.
console.log(`Hello ${name}, would you like to learn some Python today?`);
